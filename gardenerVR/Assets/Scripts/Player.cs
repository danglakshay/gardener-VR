using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    public int plantLevel;       //Represents what plant the user is currently growing.
    public string plantStage;    //Represents what stage of growing the plant the user is at.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
